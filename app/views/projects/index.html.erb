<%= render 'application/table',
  title: t('.title'),
  model_class: Project,
  records: @projects,
  columns: [:project_number, :name, :created_at],
  new_link: new_project_path %>

<%= render 'pagy' %>
