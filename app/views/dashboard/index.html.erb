  <div class="py-4">
    <h1 class="h2 mb-3 text-white"><%= t('dashboard.title') %></h1>
    <h2 class="h5 mb-3 text-white"><%= t('dashboard.subtitle') %></h2>
  </div>
  <div class="row g-4">
    <!-- Projects Card -->
    <div class="col-md-6 col-lg-4">
      <div class="card h-100">
        <div class="card-body">
          <h5 class="card-title">
            <i class="bi bi-folder"></i> <%= t('navigation.menu.projects.title') %>
          </h5>
          <p class="card-text">
            <div class="h3 mb-2"><%= Project.active.count %></div>
            <%= t('common.labels.active_projects') %>
          </p>
          <div class="mt-3">
            <%= link_to projects_path(archived: false), class: 'btn btn-outline-primary m-1 me-2' do %>
              <i class="bi bi-list"></i> <%= t('common.labels.overview') %>
            <% end %>
            <%= link_to new_project_path, class: 'btn btn-primary' do %>
              <i class="bi bi-plus"></i> <%= t('common.labels.new') %>
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <!-- Material Certificates Card -->
    <div class="col-md-6 col-lg-4">
      <div class="card h-100">
        <div class="card-body">
          <h5 class="card-title">
            <i class="bi bi-folder"></i> <%= t('navigation.menu.material_certificates.title') %>
          </h5>
          <p class="card-text">
            <div class="h3 mb-2"><%= MaterialCertificate.count %></div>
            <%= t('navigation.menu.material_certificates.total') %>
          </p>
          <div class="mt-3">
            <%= link_to material_certificates_path, class: 'btn btn-outline-primary m-1 me-2' do %>
              <i class="bi bi-list"></i> <%= t('common.labels.overview') %>
            <% end %>
            <%= link_to new_material_certificate_path, class: 'btn btn-primary' do %>
              <i class="bi bi-plus"></i> <%= t('common.labels.new') %>
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <!-- Projects Card -->
    <div class="col-md-6 col-lg-4">
      <div class="card h-100">
        <div class="card-body">
          <h5 class="card-title">
            <i class="bi bi-folder"></i> <%= t('navigation.menu.projects.archived.title') %>
          </h5>
          <p class="card-text">
            <div class="h3 mb-2"><%= Project.archived.count %></div>
            <%= t('navigation.menu.projects.archived.title') %>
          </p>
          <div class="mt-3">
            <%= link_to projects_path(archived: true), class: 'btn btn-outline-primary m-1 me-2' do %>
              <i class="bi bi-list"></i> <%= t('common.labels.overview') %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>